{
  "name": "@oaiy/bridge-client",
  "version": "0.1.0",
  "private": true,
  "description": "Reference client for the OAIY Bridge Protocol v1. Zero dependencies; browser + Node.",
  "type": "module",
  "main": "bridge-client.ts",
  "types": "bridge-client.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types tests/unit.test.ts",
    "test:live": "node --experimental-strip-types tests/live.mjs"
  }
}
