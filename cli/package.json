{
  "name": "oaiy-cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Headless CLI to run oaiy flows on a server (no GUI). Shares the oaiy-core engine with the web app via a Node host adapter.",
  "license": "Apache-2.0",
  "bin": {
    "oaiy": "bin/oaiy.mjs"
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "esbuild test/module-ctx-isolation.ts --bundle --platform=node --format=esm --outfile=dist/_ctxtest.mjs && node dist/_ctxtest.mjs && esbuild test/cross-module-isolation.ts --bundle --platform=node --format=esm --outfile=dist/_xmtest.mjs && node dist/_xmtest.mjs && esbuild test/ssrf-guard.test.ts --bundle --platform=node --format=esm --packages=external --outfile=dist/_ssrftest.mjs && node dist/_ssrftest.mjs && esbuild test/connector-module.test.ts --bundle --platform=node --format=esm --outfile=dist/_connectortest.mjs && node dist/_connectortest.mjs && node test/build-engine-tests.mjs",
    "oaiy": "node bin/oaiy.mjs"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "playwright": "^1.60.0",
    "undici": "^8.5.0"
  },
  "optionalDependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "commander": "^12.1.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.8.3"
  }
}
